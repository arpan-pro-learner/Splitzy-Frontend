/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['picsum.photos',"placehold.co"],  // Add 'picsum.photos' to allow external images
    },
  };
  
  export default nextConfig;
  